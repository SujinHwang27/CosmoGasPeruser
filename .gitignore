.dvc/cache/
.dvc/tmp/
.dvc/config.local

# Python cache files
__pycache__/
*.pyc
*.pyo
reports/

# Sensitive and temporary files
.env
logs/
nohup.out
*.log
mlartifacts/
local/
.agent/

# Claude Code user-specific settings (permissions, etc.)
.claude/settings.local.json

# Claude Code per-project auto-memory dir — local-only, never commit
# (memory content travels with the project folder via the autoMemoryDirectory
#  setting in .claude/settings.local.json)
.claude-memory/

# DVC-tracked directories (pointer files *.dvc are in git)
/mlruns
mlflow.db

# Juno HPC scratch pull-back staging area (results/ is the curated copy)
cloud_runs/
